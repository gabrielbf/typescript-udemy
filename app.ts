import "jQuery";
$("#app").css({"background-color": "green"});

// 132 More on "tsc" and the tsconfig File
// when using tsc app.ts, the tsconfig file is not used
// use -p <location> to use tsconfig file in other directory

// 133 Adding TypeScript into a Gulp Workflow
